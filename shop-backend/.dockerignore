# =============================================================================
# .dockerignore - Files to EXCLUDE from Docker build context
# =============================================================================
# WHY? When you run `docker build`, Docker sends ALL files in the directory
# to the Docker daemon. This file tells Docker what NOT to send.
# Benefits:
#   1. Faster builds (less data to transfer)
#   2. Smaller images (don't accidentally include junk)
#   3. Security (don't include .env with secrets!)
# =============================================================================

# Dependencies - we'll install fresh in the container
node_modules

# Environment files with secrets - NEVER include in image!
.env
.env.local
.env.*.local

# Git files - not needed in container
.git
.gitignore

# Editor/IDE files
.vscode
.idea
*.swp
*.swo

# Build outputs - we'll build fresh in container
.medusa

# Logs
*.log
npm-debug.log*
yarn-debug.log*

# Test files - not needed in production
integration-tests
*.test.ts
*.spec.ts
jest.config.js

# Database backups - definitely don't want these in the image!
*.sql

# Documentation
README.md
docs

# Docker files themselves
Dockerfile
Dockerfile*
docker-compose*.yml
.dockerignore

# OS files
.DS_Store
Thumbs.db
